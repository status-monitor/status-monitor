import styled from 'styled-components';
import { cardBackground } from '@app/styles/variables';

export const Card = styled.div`
  background-color: ${cardBackground};
  margin: 1rem 0;
  border-radius: 0.625rem;
`;
